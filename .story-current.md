# Story: Align docs/examples.md with real behavior

## Goal
Make docs/examples.md accurately describe what each example actually does when you run it.

## What to do
1. Read each example in examples/ and run it
2. Read docs/examples.md 
3. Fix any descriptions that don't match reality:
   - basic research: if it says "parallel" but code is serial, fix the doc
   - subprocess: if it says "cross-process" but it's inline, note it accurately  
   - coding_pipeline: if bottleneck/resilience numbers vary per run, don't hardcode them
4. Keep descriptions concise and accurate

## Acceptance criteria
- Every claim in docs/examples.md matches what `python3 examples/<name>.py` actually produces
- No "real failures, real fallbacks" overstatement — say what actually happens
- All existing tests still pass

## Files to modify
- docs/examples.md
- Possibly README.md (the "real failures, real fallbacks" line)

## Do NOT
- Add new modules
- Change any Python code
- Modify tests
